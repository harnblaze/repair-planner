# Architecture

> Статус: согласовано 2026-09-11. Код ещё не написан — документ описывает целевую архитектуру MVP.

## 1. Стек

| Слой | Решение | Обоснование |
|---|---|---|
| Framework | Next.js 16, App Router | Требование проекта; Server Components снижают объём клиентского кода. Установлена актуальная стабильная версия (16.3.4) вместо ранее предполагавшейся 15 — на момент реализации это текущий latest |
| Язык | TypeScript (strict) | |
| UI | Tailwind CSS 4 + shadcn/ui | Компоненты копируются в репозиторий, нет тяжёлой зависимости |
| Backend | Supabase (PostgreSQL, Auth, Storage) | |
| SSR-интеграция | `@supabase/ssr` | Единственный поддерживаемый способ работы с cookie-сессией в App Router |
| Формы | React Hook Form + Zod | Одна Zod-схема используется и на клиенте, и в Server Action |
| Runtime | Node.js 22 | Установлен в окружении |

Drag-and-drop в MVP не реализуется, библиотека не добавляется (см. §7).

В Next.js 16 файл `middleware.ts` переименован в `proxy.ts` (функциональность та же, изменилось только имя файла и экспортируемой функции). Проект использует новое имя.

## 2. Границы ответственности

```
Browser (Client Components)
   │  формы, модальные окна, локальное состояние
   ▼
Server Components / Server Actions
   │  чтение данных, валидация Zod, вызов Supabase от имени пользователя
   ▼
PostgreSQL (Supabase)
   │  RLS, CHECK-ограничения, триггеры, SECURITY DEFINER функции
   ▼
   данные
```

Ключевой принцип: **безопасность и целостность обеспечиваются в PostgreSQL**, а не в приложении. Приложение отвечает за UX и удобные сообщения об ошибках. Даже при полностью скомпрометированном клиенте пользователь не получит доступ к чужому проекту.

## 3. Клиент и сервер

* По умолчанию — **Server Component**.
* Client Component создаётся только там, где нужна интерактивность: формы, диалоги, фильтры, локальное состояние.
* Данные читаются в Server Components через серверный Supabase-клиент.
* Мутации выполняются **Server Actions**, которые: валидируют вход через Zod → вызывают Supabase → вызывают `revalidatePath`.
* `service_role` ключ не используется нигде в приложении. Все операции идут от имени пользователя; повышенные права нужны только внутри `SECURITY DEFINER` функций в БД.

Три клиента Supabase:

| Файл | Назначение |
|---|---|
| `lib/supabase/client.ts` | браузерный клиент (только `anon` ключ) |
| `lib/supabase/server.ts` | серверный клиент для Server Components и Server Actions |
| `lib/supabase/proxy.ts` | обновление сессии, защита маршрутов; вызывается из `proxy.ts` |

## 4. Маршрутизация и выбор проекта

Активный проект — часть URL: `/(app)/[projectId]/...`. Это даёт:

* естественную изоляцию данных в запросах;
* работающие «назад»/«вперёд» и закладки на конкретный проект;
* отсутствие скрытого глобального состояния «текущий проект».

Layout `app/(app)/[projectId]/layout.tsx` один раз проверяет доступ к проекту и отдаёт контекст (название проекта, timezone, роль) вниз по дереву. Если доступа нет — `notFound()`, без различения «не существует» и «нет доступа».

## 5. Бизнес-логика

Вынесена в `lib/business/`, не размазана по компонентам:

| Модуль | Ответственность |
|---|---|
| `working-days.ts` | является ли день рабочим; следующий/предыдущий рабочий день; неделя доски |
| `task-planning.ts` | планирование задачи на день, перенос, снятие с дня |
| `materials.ts` | правила расхода, признак низкого остатка, форматирование количеств |
| `dates.ts` | преобразования `date` ↔ строка с учётом `project.timezone` |

Правила, которые обязаны жить в БД (а не только здесь): пересчёт остатка, запись журнала движений, кеш планового дня задачи.

## 6. Доска проекта

Доска — горизонтальный набор колонок:

```
[ Текущие заявки ] [ Пн 14.09 ] [ Вт 15.09 ] [ Ср 16.09 ] [ Чт 17.09 ] [ Пт 18.09 ] │ [ Материалы к заказу ] [ Напоминания ] [ Мероприятия ]
        задачи            задачи из task_schedule                                                   board_items
```

Источники данных различаются, а компонент колонки — **один**:

| Колонка | Источник |
|---|---|
| Текущие заявки | `tasks`, у которых нет записей в `task_schedule` и статус не `completed`/`cancelled` |
| Рабочий день | `task_schedule` за конкретную дату + связанные задачи |
| Дополнительные списки | `board_items` соответствующего `board_lists` |

«Текущие заявки» **не** являются записью в `board_lists`: это задачи со всей обвязкой (исполнители, материалы, категория), и попытка уложить их в универсальную модель заметок обеднила бы модель задач. Универсальная модель списков применяется там, где она действительно универсальна — к простым чек-листам, что сразу даёт возможность пользовательских списков в будущем.

Неделя доски вычисляется от «сегодня» в timezone проекта; предусмотрена навигация по неделям.

## 7. Drag-and-drop

Добавлен после MVP (`@dnd-kit/core` + `@dnd-kit/sortable` + `@dnd-kit/utilities`; стабильная ветка 6.x — `@dnd-kit/react` на момент выбора ещё 0.x). Бизнес-правила — product-requirements.md §4.4.

* **Серверная часть не изменилась по форме:** `board/page.tsx` — Server Component, загружает данные и вычисляет для каждой карточки `isHistory` / `canChangeDay` (`describeOccurrence` в `lib/business/task-planning.ts`).
* **Клиент:** `board/week-board.tsx` — один `DndContext` на неделю и «Текущие заявки»; `board/board-list.tsx` — отдельный вложенный `DndContext` на каждый дополнительный список. Общие сенсоры, объявления для скринридера и подавление клика после перетаскивания — `board/dnd.ts`.
* **Мутации:** server actions в `board/actions.ts` → Zod (`lib/validation/board-move.ts`) → один вызов RPC. Никаких последовательностей независимых запросов: перестановка, смена дня и пересчёт `planned_date` атомарны в БД (database.md §5.8).
* **Отклик:** `useOptimistic` + `startTransition` — карточка переезжает сразу; при ошибке показывается toast, состояние само возвращается к данным сервера после завершения transition.
* **Проверки правил дублируются:** на клиенте — для мгновенного отказа с объяснением, в RPC — как источник истины.
* **Ввод:** мышь — после сдвига 6px (клик по карточке по-прежнему открывает задачу); тач — долгое нажатие 250ms (прокрутка не ломается); клавиатура — пробел (Enter на карточке-ссылке открывает задачу). Все действия доступны и без перетаскивания — в карточке задачи.

## 8. Визуальный слой

Оформление задано дизайн-референсом `docs/redesign.md` (и `docs/Repair Planner.dc.html`, который при расхождении считается источником истины). Референс описывает только внешний вид: структура экранов, роутинг и бизнес-логика им не затрагиваются.

* **Токены.** Палитра, границы, радиусы и цвета статусов объявлены один раз в `app/globals.css` — блоком `@theme` (утилиты вида `bg-surface`, `text-meta`, `border-line-subtle`) и через переменные shadcn в `:root`, которые указывают на те же значения. Благодаря этому `Card`, `Button`, `Input` и `Select` получают новую палитру без точечных переопределений в компонентах. Хардкод hex-значений в разметке допускается только для единичных оттенков ховера, которых нет в таблице токенов.
* **Типографика.** `IBM Plex Sans` для текста и `IBM Plex Mono` для дат, счётчиков и остатков — подключены через `next/font/google` в `app/layout.tsx` (подмножества `latin` + `cyrillic`), базовый размер 13px.
* **Контролы.** Единая метрика: высота 30px, радиус 7px, размер текста 12.5px, фокус — кольцо `0 0 0 3px rgba(47,85,200,0.12)`. Панели — радиус 10px, бейджи — 4px.
* **Иконки.** Инлайновые SVG в `components/common/icons.tsx` (viewBox 16×16, stroke 1.4–1.6). Отдельная icon-библиотека ради этого не вводится; `lucide-react` остаётся только там, где его использует shadcn.
* **Статусы и остатки.** Цвета бейджей задач живут в `components/common/status-badge.tsx` и ключуются стабильными значениями `task_status`, а не русскими подписями. Состояния остатка материала (низкий, отрицательный) — в `components/common/balance-badge.tsx`; отрицательный остаток оформлен как допустимое бизнес-состояние, а не как системная ошибка.
* **Адаптивность.** Только desktop. Композиция доски — пять колонок и четыре нижние панели — не переносится и не схлопывается на 1440 / 1280 / 1024px; при сужении уменьшаются только gap и внутренние padding. Сетки заданы через `minmax(0, 1fr)`, иначе колонки распирает длинный текст.
* **Нижние панели.** Общая оболочка (`Panel`, `PanelHeader`, `PanelEmpty`, `PANEL_FORM_CLASS`) лежит в `app/(app)/[projectId]/board/panel.tsx` — четыре панели не реализуются независимо друг от друга.

## 9. Работа с ошибками

* Server Action возвращает дискриминированный результат `{ ok: true, data } | { ok: false, error: string }`, где `error` — сообщение для человека.
* Технические детали логируются на сервере.
* Коды ошибок PostgreSQL (уникальность, нарушение FK, нарушение RLS) маппятся в понятные фразы централизованно, в `lib/errors.ts`.
* Результат мутации на клиенте показывается через toast (`sonner`, `components/ui/sonner.tsx`, `<Toaster />` подключён в корневом `app/layout.tsx`): `toast.error(...)` — всегда при `!result.ok`; `toast.success(...)` — при явном действии кнопкой (создание, сохранение, удаление, переключение статуса). Для непрерывного инлайн-редактирования (автосохранение по `onBlur`/`onChange`: поля задачи, выбор исполнителей, дата плана) — только `toast.error`, без toast об успехе, чтобы не быть навязчивым.
* Ошибки при загрузке данных страницы (не мутации) ловятся `error.tsx`-границами: `app/error.tsx` — на весь корневой layout, `app/(app)/error.tsx` — на всё аутентифицированное приложение, включая сбой в `[projectId]/layout.tsx`. Пользователю показывается общая фраза и кнопка «Попробовать снова» (`reset()`), без деталей ошибки.
* Отсутствие/недоступность проекта (`notFound()` в `[projectId]/layout.tsx`) обрабатывает `app/(app)/not-found.tsx` — важно: `notFound()`, вызванный в самом `layout.tsx`, подхватывается `not-found.tsx` из **родительского** сегмента, а не из той же папки (Next.js не может обернуть упавший в рендере layout его же соседним файлом). `notFound()` из страниц внутри `[projectId]` (например, несуществующая задача) обрабатывает `app/(app)/[projectId]/not-found.tsx` — он рендерится уже внутри шапки проекта, так как сам `[projectId]/layout.tsx` в этом случае успешно отрендерился.
* `loading.tsx` (skeleton на основе `components/ui/skeleton.tsx`) добавлен для основных экранов с загрузкой данных: доска, заявки, справочники, карточка задачи, список проектов.

## 10. Производительность

* Запросы всегда ограничены проектом и, для доски, диапазоном дат недели.
* Доска загружается одним-двумя запросами с явным списком колонок, без `select *`.
* Индексы под каждый шаблон запроса описаны в `docs/database.md`.
* Пагинация применяется в архиве задач и в журнале движений материалов, не на доске.
* `revalidatePath` — основной механизм инвалидации; клиентский кеш-слой в MVP не вводится.

## 11. Структура проекта

```
app/
  (auth)/
    login/ register/ forgot-password/ reset-password/
  (app)/
    layout.tsx                      проверка сессии, общая навигация, выход
    actions.ts                      signOutAction (общий для всех страниц (app))
    profile/                        профиль пользователя (full_name)
    projects/                       список, создание и переключение проектов
    [projectId]/
      layout.tsx                    проверка доступа, контекст проекта
      board/                        главная доска: week-board.tsx (DnD недели), dnd.ts, panel.tsx
      tasks/                         список заявок, быстрое создание
      tasks/[taskId]/               карточка задачи
      materials/                    справочник и остатки
      reports/                      отчёт по расходу материалов (page, data.ts, export/route.ts — CSV)
      executors/
      categories/
      settings/                     название проекта, timezone
  auth/confirm/route.ts             обработка ссылок из писем Supabase Auth (token_hash + type)
  api/                              только при реальной необходимости
components/
  ui/                               shadcn/ui + native-select.tsx
  common/                           icons.tsx, status-badge.tsx, balance-badge.tsx,
                                    project-nav.tsx, empty-state.tsx, sign-out-button.tsx
lib/
  supabase/                         client.ts, server.ts, proxy.ts
  business/                         working-days.ts, task-planning.ts, material-report.ts, dates.ts
  validation/                       Zod-схемы, общие для клиента и сервера
  errors.ts
  types/database.ts                 типы, сгенерированные Supabase CLI
supabase/
  migrations/
  config.toml
docs/
proxy.ts
```

## 12. Тестирование и проверки

| Что | Чем |
|---|---|
| Бизнес-функции (рабочие дни, перенос, расчёт остатков) | Vitest, unit-тесты |
| Типы | `tsc --noEmit` |
| Стиль | ESLint (конфигурация Next.js) |
| RLS | отдельный скрипт/тест: пользователь A не видит и не меняет данные пользователя B |

Тесты RLS обязательны: это единственный слой, который нельзя проверить глазами.

## 13. Переменные окружения

```
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY
```

`SUPABASE_SERVICE_ROLE_KEY` в приложении не используется; если он когда-либо понадобится для служебных скриптов — только в серверном окружении и никогда не в коде, попадающем в браузер. Файл `.env.local` в git не коммитится, в репозитории хранится `.env.example`.
